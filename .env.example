# API keys and other machine-local settings live in _local/.env (not committed to Git).
#
# 1. Copy _local/.env.example to _local/.env
# 2. Uncomment and paste your keys
# 3. Restart npm run dev
#
# See _local/README.md for the full list of local-only files.
